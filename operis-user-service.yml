
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: password
    url: jdbc:postgresql://localhost:54322/operis_user_db

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:900/realms/operis

  rabbitmq:
    host: localhost
    port: 5672
    username: operis
    password: password

eureka:
  instance:
    instance-id: ${spring.application.name}:${random.value}
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka

keycloak:
  server-url: http://localhost:9000
  realm: operis
  client-id: operis-identity-service
  client-secret: tLWqWvt7tDUxmRoVCnwAAMNYe8SA8Pwu
